const Discord = require('discord.js');
const config = require('../config.json');

module.exports = {
    run: async(client, message, arguments) => {

    },

    name: "apply",
    aliases: [],
    category: "Important",
    information: "Apply for Staff on Dawn Realms."
}